#include <stdio.h>
#include <math.h>

int main() {
  enum unidade_e
  { // agrupa as constantes
     preto = 0,
     marrom = 1,
     vermelho = 2,
     laranja = 3,
     amarelo = 4,
     verde = 5,
     azul = 6,
     violeta = 7,
     cinza = 8,
     branco = 9,
     dourado = 5,
     prata = 10
  };
  struct resistor_t {
    int quantidade_de_cores;
    enum unidade_e cores[5];
    float resistencia;
    float resistencia_máxima;
    float resistencia_mínima;
  };
  int numero_de_faixas[5];
  
  printf("O resistor tem 4 ou 5 cores? ");
   int deu_certo = scanf("%i”, &numero_de_faixas);
}
}
if (4) {
  printf(Digite a primeira cor);
  printf(Digite a segunda cor);
  printf(Digite a terceira cor);
  printf(Digite a quarta cor);
}
if (5) {
  printf(Digite a primeira cor);
  printf(Digite a segunda cor);
  printf(Digite a terceira cor);
  printf(Digite a quarta cor);
  printf(Digite a quinta cor);
  else

      struct resitores_t { // agrupa variaveis
    char nome[81];
    int quantidade;
    int unidade;
    float valor;
    float valor;
  };

  float total = 0.0f;
  struct;
  for () {

    printf("Entre com a primeira cor: ");
    int deu_certo = scanf("%f", primeira[i].cor);
    printf("Entre com a segunda cor: ");
    int deu_certo = scanf("%f", segunda[i].cor);
    printf("Entre com a terceira cor: ");
    int deu_certo = scanf("%f", terceira[i].cor);
    printf("Entre com a quarta cor: ");
    int deu_certo = scanf("%f", quarta[i].cor);
    printf("Entre com a quinta cor: ");
    int deu_certo = scanf("%f", quinta[i].cor);

    primeira cor;
  }
  printf("-------------------------------\n");
  printf("\n      Valor da resistencia\n");
  printf("------------------------\n");
  printf("Item          Ohms\n");

  return 0;
}
